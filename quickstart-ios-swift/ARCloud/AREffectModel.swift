import UIKit

class AREffectModel {
    var title: String
    var previewUrl: String
    
    init(title: String, previewUrl: String) {
        self.title = title
        self.previewUrl = previewUrl
    }
}
